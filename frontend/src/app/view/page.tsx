"use client";
import ScrollableCarousel from "@/components/scrollable-carousel";

export default function Reels() {
    return (
        <main>
            <ScrollableCarousel />
        </main>
    );
}
