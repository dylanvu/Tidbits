"use client";
import ScrollableCarousel from "@/components/ScrollableCarousel";

export default function Reels() {
    return (
        <main>
            <ScrollableCarousel />
        </main>
    );
}
